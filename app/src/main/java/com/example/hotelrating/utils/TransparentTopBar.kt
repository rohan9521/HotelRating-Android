package com.example.hotelrating.utils

import android.app.Activity
import androidx.activity.ComponentActivity

interface TransparentTopBar {
    fun makeTopBarTransparent(activity:ComponentActivity)
}